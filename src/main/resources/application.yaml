ktor:
  application:
    modules:
      - org.delcom.ApplicationKt.module
  deployment:
    port: 8080
  jwt:
    secret: ${JWT_SECRET}
  database:
    host: ${DB_HOST}
    port: ${DB_PORT}
    name: ${DB_NAME}
    user: ${DB_USER}
    password: ${DB_PASSWORD}